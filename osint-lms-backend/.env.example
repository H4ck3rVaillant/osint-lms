# ===========================================
# CONFIGURATION BACKEND - CyberOSINT Academy
# ===========================================

# Port du serveur
PORT=3000

# Secret JWT (CHANGE THIS IN PRODUCTION!)
# Générer un secret fort avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_me_in_production

# URL du frontend (CORS)
FRONTEND_URL=http://localhost:5173

# Base de données
DB_PATH=./db/users.db

# Mode environnement
NODE_ENV=development
