# =============================================
# CONFIGURATION BACKEND — CyberOSINT Academy
# Copiez ce fichier en .env et remplissez les valeurs
# NE JAMAIS committer le fichier .env !
# =============================================

# --- Serveur ---
PORT=3000
NODE_ENV=development

# --- JWT ---
# Générer avec : node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGEZ_MOI_AVEC_UNE_VALEUR_FORTE_ET_UNIQUE

# --- Base de données ---
# En production sur Render : DATABASE_URL est injectée automatiquement
# En développement local (PostgreSQL) :
DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=osint_lms
DB_USER=postgres
DB_PASSWORD=postgres

# --- CORS ---
# URL complète du frontend déployé sur Render (ex: https://osint-lms.onrender.com)
FRONTEND_URL=http://localhost:5173
